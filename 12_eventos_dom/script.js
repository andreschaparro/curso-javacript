function mostrar(){
    // buscamos el elemento con id igual a "seleccionable" y le cambia el texto por la fecha actual
    document.getElementById('seleccionable').innerHTML = Date();
}

// Listado de eventos: https://www.w3schools.com/tags/ref_eventattributes.asp